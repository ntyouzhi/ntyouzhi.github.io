# 体育课标 × AI 演示工作台 · 交付说明

日期：2026-09-05　｜　用途：向体育教师讲解"AI 如何结合《义务教育体育与健康课程标准（2022年版）》完成体育教学工作"

## 一、这是什么

一个单页网页（纯前端，无服务器、无数据库），用**掷实心球**这一个动作，把 AI 结合课标的三层做法演示给体育老师看：

| Tab | 内容 | 对应的课标抓手 |
|---|---|---|
| ① 开场怎么说 | 为什么体育适合做 AI 演示 + 12 分钟演示路线 | 全局 |
| ② 动作分解 | 课标 12 个字"蹬地—满弓—挥臂—拨指"变成可播放分帧图 + 实时关节角；"怎样获得最适宜的出手角度"变成可拖的抛物线曲线 | 四、课程内容（三）专项运动技能·田径类运动；（五）跨学科主题学习 4 |
| ③ AI 动作诊断 | 摄像头实时姿态识别（MediaPipe Pose）/ 离线示例双输入 → 几何计算出手角、屈膝度、满弓度、发力时序 → 规则诊断 → AI 生成学生指导语 | 同上 + 六、课程实施（二）评价建议 1.(3)③ |
| ④ 运动负荷自检 | 填一节课的时间分配 → 自动算群体密度、个体密度、平均心率、体能时长，判四项红线，一键"AI 重排这一节课" | 六、课程实施（一）教学建议 3.(4) |
| ⑤ 迁移与边界 | 十个课标板块的可迁移清单 + AI 擅长什么 / 绝不该碰什么 | 全局 |

## 二、怎么打开

**双击 `260905-01_体育AI演示工作台.html`** 即可（Chrome 或 Edge）。动画、示例诊断、负荷计算、AI 重排、本地指导语生成——**全部离线可用，U 盘拷走就能讲。**

只有两种情况需要额外条件：

1. **摄像头实时识别**：浏览器在 `file://` 下禁止调用摄像头。需要现场演示这一项时，在该文件夹下起一个本地服务，再用 `http://127.0.0.1:8765/260905-01_体育AI演示工作台.html` 打开：
   ```
   python -m http.server 8765
   ```
   首次点"开启摄像头"会联网下载约 9 MB 的 MediaPipe 姿态模型；**校园网受限或加载失败会自动回到离线示例，并在页面上写明失败原因**，不会卡住演示。
2. **AI 生成指导语（可选）**：点"⚙ 设置 API Key"填入 DeepSeek Key 即可让话术更自然。Key 只存在本机浏览器 localStorage。**不填也行**——默认走本地模板，效果同样成立。

## 三、演示脚本（12 分钟，Tab①里也有）

1. **抛痛点（1′）**：问老师"您上一节掷实心球课，全班出手角度大概多少？平均心率多少？"——多数回答是"感觉还行"。
2. **Tab②（3′）**：点"播放动作链"，指着关节角说：这不是我编的，是骨骼模型算出来的。再把出手速度拖到 11 m/s，看最优角变化 → 引出"这就是课标要的跨学科探究任务"。
3. **Tab③（4′）**：先点"只挥臂不蹬地"，一秒出 4 条诊断；再点"标准示范"对比 92 分。**重点讲三层分工**：识别（在哪）→ 几何计算（多少度，确定性数学，不会胡说）→ 语言模型（怎么说）。然后开摄像头请一位老师侧身做一次。
4. **Tab④（3′）**：默认就是那节"不出汗的课"，四项全红；点"AI 重排这一节课" → 全绿，并展示重排逻辑是规则不是玄学。点"复制诊断结论"可直接贴进听课记录。
5. **Tab⑤（1′）**：收尾讲边界——安全保护、动作示范、体育品德评价永远属于老师。

## 四、已做的验证（不是"看起来对了"）

| 测试 | 文件 | 覆盖 | 结果 |
|---|---|---|---|
| 引擎离线自测 | `tests/260905-01_selftest.js` | 5 个示例动作的病灶命中、球离手时序、骨架越界、关节角物理合理性、抛体最优角、MediaPipe 关键点反推自洽、负荷四项红线、AI 重排收敛 | **40 项断言全部通过** |
| 端到端交互 | `tests/260905-02_e2e.py` | 真实 Chromium 逐个点击：播放/逐帧/拖动滑杆/5 个示例切换/时间轴/本地指导语/提示词/负荷表增删改/重排/复制/五页切换 | **56 项断言全部通过，全程无 JS 报错** |
| 摄像头链路 | `tests/260905-03_camera.py` | 伪造摄像头设备实测"开模型→取流→识别循环→指标更新"全链路 | **跑通**（模型加载约 6 s） |
| 离线直开 + 多分辨率 | 脚本内联 | `file://` 双击 + 1440 / 1280 / 1024 三档，横向溢出与报错检测 | **四页均无溢出、无报错** |

截图留证在 `测试截图/`（01～07，含重排前后对比与三档分辨率）。

重跑方式：
```
node tests/260905-01_selftest.js
python tests/260905-02_e2e.py
python -m http.server 8765 &   # 然后 python tests/260905-03_camera.py
```

## 五、过程中修掉的两个真问题（值得跟老师讲）

1. **"AI 重排"一开始并不真达标**：重排后群体密度只有 65%、心率 128。原因是补进去的体能环节又被"总时长压回 40 分钟"的逻辑削掉了。已改为体能/比赛环节受保护 + 增加收敛补足环，现在从 18% 的课能自动排到 85% 且四项全绿。**教训：给老师看的东西，数字必须真算得出，不能只是"画成达标"。**
2. **摄像头报 "ModuleFactory not set."**：MediaPipe 的 wasm 加载器是 UMD 脚本、没有 ESM 导出，动态 `import()` 拿不到它。改法：先用传统 `<script>` 注入全局，再显式传 wasm 路径；并加 GPU 失败自动退 CPU。

## 六、想换成别的项目怎么改

- 换动作：改 `assets/260905-01_pose.js` 里的 `BASE` / `variantFrames`（五个关节的角参数关键帧）与 `diagnose()` 的阈值。注释已标好位置。
- 换负荷口径：改 `assets/260905-02_loadcore.js` 的 `R`（红线数值）、`INT`（各强度对应心率）、`PLAN`（示例课）。
- 计算内核与界面分离：`loadcore` / `pose` 是纯函数，UI 只负责画，所以自测能在 Node 里直接跑同一份代码。
